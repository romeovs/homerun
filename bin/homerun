#!/usr/bin/env sh

# get dir of homerun
export homerun=$(dirname "$(dirname "$0")")

# source functions
. "$homerun/lib/defaults.sh"
. "$homerun/lib/functions.sh"


# get command
task="$1"
shift

if [ -z "$task" ]; then
  runtask help
else
  runtask "$task" "$@"
fi
